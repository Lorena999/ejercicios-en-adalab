'use strict'
const numbers = [4, 2, 7, 8, 6];
numbers[5] = 10;
let acc = 0;
const
/*for (let i = 0; i < numbers.length; i++) {
  acc += numbers[i]/6;
}
console.log('La media es ' + acc);*/
for (let i = 0; i < numbers.length; i++) {
  function averange (numbers) {
    return acc += numbers[i]/numbers.length
  }
}
console.log(averange);