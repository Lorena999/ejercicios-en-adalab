'use strict'

'use strict';

function box-sizing (border-box, width, padding, border-size) {
  if (border-box === true) {
    return 'El ancho del contenido es:' + width + ' px y el ancho total de la caja es: ' + he + 'px';
   } else {
      return 'El ancho del contenido es:' + width + ' px y el ancho total de la caja es: ' + border-size + 'px';
    }
  

  

}